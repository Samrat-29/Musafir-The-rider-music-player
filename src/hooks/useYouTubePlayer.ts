// Kept as a compatibility export for code that imported the original hook.
export { useMusicPlayer as useYouTubePlayer } from './useMusicPlayer';
export type { PlayerState, Track } from '../types/music';
